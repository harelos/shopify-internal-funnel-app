# Local runtime only. Do not add real Shopify credentials until an owner authorizes a dev-store connection.
APP_PORT=3000
ALLOWED_SHOP_DOMAIN=example.myshopify.com
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/funnel_control?schema=public
ENABLE_LOCAL_SHOPIFY_ADAPTERS=false
# Fixture-only secret; never enable these routes against an internet-facing deployment.
SHOPIFY_WEBHOOK_SECRET=
