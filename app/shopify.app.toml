# Local scaffold only. Do not run `shopify app dev`, link, or deploy without owner approval.
client_id = "REPLACE_WITH_DEV_DASHBOARD_CLIENT_ID"
name = "Funnel Control Internal"
application_url = "http://localhost:3000"
embedded = true

[build]
automatically_update_urls_on_dev = false
include_config_on_deploy = false

[access_scopes]
scopes = "read_orders,write_app_proxy,write_pixels,read_customer_events"

[auth]
redirect_urls = [ "http://localhost:3000/auth/callback" ]

[webhooks]
api_version = "2026-04"
