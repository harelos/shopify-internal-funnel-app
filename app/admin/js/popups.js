document.addEventListener("DOMContentLoaded",()=>{const tabs=[...document.querySelectorAll("[data-tab]")],panels=[...document.querySelectorAll("[data-panel]")];function setTab(name){tabs.forEach(t=>t.classList.toggle("btn-primary",t.dataset.tab===name));panels.forEach(p=>p.classList.toggle("hidden",p.dataset.panel!==name));}tabs.forEach(t=>t.addEventListener("click",()=>setTab(t.dataset.tab)));document.getElementById("btn-popup-save")?.addEventListener("click",()=>{document.getElementById("popup-save-status").textContent="Draft captured in UI only on this scaffold branch. No storefront code was published.";});});
